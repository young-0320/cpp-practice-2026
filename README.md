# git upload(current working directory)
    git add .
    git commit -m "commit message"
    git push

# git upload all(root directory)
    git add -A
    
# git 상태 확인
    git status

# git sync
    git pull
    
